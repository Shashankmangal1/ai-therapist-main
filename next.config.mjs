// Auto-generated to replace `next.config.ts` for Next.js runtime compatibility.
// If you need TypeScript types here, keep `next.config.ts` for editor-only types
// but Next.js requires a JS/MJS config file to load at runtime.

const nextConfig = {
  /* config options here */
};

export default nextConfig;
